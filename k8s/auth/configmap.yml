apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-config
  namespace: warden
data:
  HTTP_PORT: ":8082"
  ISSUER: "warden-auth"
  AUDIENCE: "warden-engine"
  PRIVATE_KEY_PATH: "/etc/auth/keys/private.pem"
